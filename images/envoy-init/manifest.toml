[metadata]
tier = "standard"
name = "envoy-init"
version = "1.38.0"
description = "Evergreen hardened Envoy init - initialization mode for bootstrap configuration"

[build]
base = "scratch"
user = "65532:65532"
stopsignal = "SIGTERM"

[source]
type = "package-manager"

[runtime]
entrypoint = ["/usr/local/bin/envoy", "--mode", "init"]

[ports]
expose = [9901, 9101]

[labels]
"org.opencontainers.image.description" = "Evergreen hardened Envoy init - initialization mode for bootstrap configuration"
"org.opencontainers.image.title" = "envoy-init"
"org.opencontainers.image.version" = "${VERSION}"
"evergreen.base.image" = "scratch"
"evergreen.health.type" = "exec"
"evergreen.image.category" = "proxy"
"evergreen.image.status" = "functional"
"evergreen.image.tier" = "standard""evergreen.metrics.native" = "ztunnel"
