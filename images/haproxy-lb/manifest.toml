[metadata]
tier = "standard"
name = "haproxy-lb"
version = "2.8.5"
description = "Evergreen hardened HAProxy LB - dedicated load balancer configuration variant"

[build]
base = "scratch"
user = "65532:65532"
stopsignal = "SIGTERM"

[source]
type = "package-manager"

[runtime]
entrypoint = ["/usr/local/sbin/haproxy", "-f", "/dev/stdin"]

[ports]
expose = [80, 9101]

[labels]
"org.opencontainers.image.description" = "Evergreen hardened HAProxy LB - dedicated load balancer configuration variant"
"org.opencontainers.image.title" = "haproxy-lb"
"org.opencontainers.image.version" = "${VERSION}"
"evergreen.base.image" = "scratch"
"evergreen.health.type" = "exec"
"evergreen.image.category" = "load-balancer"
"evergreen.image.status" = "functional"
"evergreen.image.tier" = "1"
"evergreen.metrics.native" = "ztunnel"
