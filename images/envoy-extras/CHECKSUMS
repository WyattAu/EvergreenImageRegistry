# CHECKSUMS - envoy-extras
# Generated: 2026-04-19
# Status: PENDING_VERIFICATION
#
# NOTE: This image variant shares its download with the primary image: envoy
# The CHECKSUMS file in envoy/ takes precedence for verification.
#
# IMPORTANT: These checksums must be verified before use.
# Method: Download the binary on an air-gapped machine, compute SHA256,
# then compare against upstream checksum file if available.
#
# Update protocol:
# 1. Download binary from URL
# 2. Compute: sha256sum <file>
# 3. Compare with upstream sha256sums.txt if available
# 4. Cross-validate with second team member
# 5. Update EXPECTED_SHA256 below
# 6. Submit PR with CHECKSUMS update

[metadata]
image = "envoy-extras"
version = "1.31.0"
created = "2026-04-19"
last_verified = "2026-04-20"
verification_method = "github_release"
verifier = "PENDING"
primary_image = "envoy"

[download]
url = "https://github.com/envoyproxy/envoy/releases/download/v1.31.0/envoy-1.31.0.tar.gz"
filename = "envoy-1.31.0.tar.gz"

[checksum]
# SHA256 of the downloaded archive/binary
# Leave as "PENDING" until verified
expected_sha256 = "PENDING"

[upstream_checksum]
# If upstream provides a checksum file, note the URL here
url = "https://github.com/envoyproxy/envoy/releases/download/v1.31.0/checksums.txt.asc"
format = "sha256"
