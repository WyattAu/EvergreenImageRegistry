name: Forgejo Runner Build
on:
  workflow_dispatch:
  push:
    branches: [main]
    paths: ["images/forgejo-runner-image/**"]

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd
      - run: echo "Forgejo runner build placeholder"
