# Default values for promtail
image:
  name: promtail
  tag: "latest"
  pullPolicy: IfNotPresent

tier: standard
replicas: 1

security:
  readOnlyRootFs: true

service:
  type: ClusterIP
  port: 8080

env: {}
envFrom: []

persistence:
  enabled: false
