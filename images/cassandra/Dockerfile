FROM scratch
LABEL org.opencontainers.image.title="CASSANDRA" \
      org.opencontainers.image.description="Sovereign hardened CASSANDRA (stub - upstream binary unavailable at version 4.1.4)" \
      sovereign.image.status="no-upstream-binary" \
      sovereign.image.note="No pre-built binary available for version 4.1.4. Update VERSION when upstream publishes new release."
