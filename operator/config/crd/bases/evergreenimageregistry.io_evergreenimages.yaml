apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: evergreenimages.evergreenimageregistry.io
spec:
  group: evergreenimageregistry.io
  names:
    kind: EvergreenImage
    listKind: EvergreenImageList
    plural: evergreenimages
    shortNames:
    - ei
    - eimg
    singular: evergreenimage
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.image
      name: Image
      type: string
    - jsonPath: .spec.tag
      name: Tag
      type: string
    - jsonPath: .spec.autoUpdate
      name: Auto Update
      type: boolean
    - jsonPath: .status.sbomStatus
      name: SBOM
      type: string
    - jsonPath: .status.complianceStatus
      name: Compliance
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1
    schema:
      openAPIV3Schema:
        description: EvergreenImage is the Schema for the evergreenimages API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: EvergreenImageSpec defines the desired state of EvergreenImage
            properties:
              autoUpdate:
                default: false
                description: Enable automatic updates on new versions
                type: boolean
              compliance:
                description: Compliance requirements
                properties:
                  fipsRequired:
                    default: false
                    description: Require FIPS compliance
                    type: boolean
                  requireSBOM:
                    default: true
                    description: Require valid SBOM
                    type: boolean
                  requireVEX:
                    default: false
                    description: Require VEX document
                    type: boolean
                type: object
              image:
                description: Image reference (e.g., ghcr.io/wyattau/evergreenimageregistry/redis)
                type: string
              notifications:
                description: Notification configuration
                properties:
                  pagerduty:
                    default: false
                    description: Enable PagerDuty alerts
                    type: boolean
                  slack:
                    description: Slack channel for notifications
                    type: string
                type: object
              tag:
                default: latest
                description: Image tag to track
                type: string
              updateStrategy:
                default: rolling
                description: Kubernetes update strategy
                enum:
                - rolling
                - recreate
                type: string
            required:
            - image
            type: object
          status:
            description: EvergreenImageStatus defines the observed state of EvergreenImage
            properties:
              complianceStatus:
                description: Compliance status
                enum:
                - passing
                - failing
                - unknown
                type: string
              conditions:
                description: Conditions
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this resource."
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition
                        transitioned from one status to another.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating details
                        about the transition.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation
                        that the condition was set based upon.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: reason contains a programmatic description indicating
                        why the condition is set to the current Status.
                      maxLength: 256
                      minimum: 1
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: type of condition in CamelCase.
                      maxLength: 317
                      minimum: 1
                      pattern: ^[a-z]([a-z0-9]*[a-z0-9])?$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              currentDigest:
                description: Current image digest
                type: string
              currentTag:
                description: Current image tag
                type: string
              lastUpdated:
                description: Last update timestamp
                format: date-time
                type: string
              sbomStatus:
                description: SBOM status
                enum:
                - valid
                - missing
                - stale
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
