FROM scratch
LABEL org.opencontainers.image.title="KEYCLOAK" \
      org.opencontainers.image.description="Sovereign hardened KEYCLOAK (stub - upstream binary unavailable at version 26.0.5)" \
      sovereign.image.status="no-upstream-binary" \
      sovereign.image.note="No pre-built binary available for version 26.0.5. Update VERSION when upstream publishes new release."
