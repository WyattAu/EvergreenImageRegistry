# Default values for oauth2-proxy
image:
  name: oauth2-proxy
  tag: "latest"
  pullPolicy: IfNotPresent

tier: standard
replicas: 1

security:
  readOnlyRootFs: true

service:
  type: ClusterIP
  port: 8080

env: {}
envFrom: []

persistence:
  enabled: false
