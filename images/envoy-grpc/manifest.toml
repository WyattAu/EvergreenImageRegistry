[metadata]
tier = "standard"
name = "envoy-grpc"
version = "1.38.0"
description = "Evergreen hardened Envoy gRPC - Envoy proxy preconfigured for gRPC routing"

[build]
base = "scratch"
user = "65532:65532"
stopsignal = "SIGTERM"

[source]
type = "package-manager"

[runtime]
entrypoint = ["/usr/local/bin/envoy"]

[ports]
expose = [15001, 9101]

[labels]
"org.opencontainers.image.description" = "Evergreen hardened Envoy gRPC - Envoy proxy preconfigured for gRPC routing"
"org.opencontainers.image.title" = "envoy-grpc"
"org.opencontainers.image.version" = "${VERSION}"
"evergreen.base.image" = "scratch"
"evergreen.health.type" = "exec"
"evergreen.image.category" = "proxy"
"evergreen.image.status" = "functional"
"evergreen.image.tier" = "standard""evergreen.metrics.native" = "ztunnel"
