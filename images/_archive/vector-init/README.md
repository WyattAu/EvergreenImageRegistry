# Vector-init (ARCHIVED)

Archived 2026-09-09.

Reason: upstream `timberio/vector` on Docker Hub is gone entirely
(404 at the registry for all tags, including `:latest`). Vector moved
distribution channels; no public timberio/vector image remains.
Not deployed in the SimpleInfrastructureStack. (The `vector` image
itself builds from upstream release tarballs and is unaffected.)
