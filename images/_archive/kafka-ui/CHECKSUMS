# CHECKSUMS - kafka-ui
# Generated: 2026-05-03T15:41:21Z
# Status: REQUIRES_VERSION
# Reason: Repo moved from provectuslabs/kafka-ui to provectus/kafka-ui; ARG VERSION has no default
#
# Dockerfile uses ARG VERSION without a default value.
# Checksum cannot be populated without knowing the version.
# URL template:
#   https://github.com/provectus/kafka-ui/releases/download/v${VERSION}/kafka-ui-v${VERSION}-linux-amd64.tar.gz
# Checksums template:
#   https://github.com/provectus/kafka-ui/releases/download/v${VERSION}/checksums.txt
# Action: Set a default VERSION in Dockerfile, then re-run checksum population.

[metadata]
image = "kafka-ui"
version = "REQUIRES_DEFAULT"
created = "2026-05-03T15:41:21Z"
last_verified = ""
verification_method = "REQUIRES_VERSION"
reason = "Repo moved from provectuslabs/kafka-ui to provectus/kafka-ui; ARG VERSION has no default"

[checksum]
expected_sha256 = "REQUIRES_VERSION"
