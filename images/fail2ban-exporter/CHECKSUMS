# CHECKSUMS - fail2ban-exporter
# Status: PENDING
#
# IMPORTANT: These checksums are verified against upstream sources.
# To re-verify:
# 1. Download tarball from URL below
# 2. Compute: sha256sum <file>
# 3. Compare with expected_sha256 below
# 4. Cross-validate against upstream_checksum URL if available
# 5. Submit PR with any changes

[metadata]
image = "fail2ban-exporter"
version = "0.10.0"
created = "2026-04-21T00:00:00Z"
last_verified = ""
verification_method = "manual"

[download]
url = "https://github.com/xperimental/fail2ban-exporter/releases/download/v0.10.0/fail2ban-exporter-0.10.0-linux-amd64.tar.gz"
filename = "fail2ban-exporter-0.10.0-linux-amd64.tar.gz"

[checksum]
expected_sha256 = "PENDING"

[upstream_checksum]
url = "https://github.com/xperimental/fail2ban-exporter/releases/download/v0.10.0/fail2ban-exporter-0.10.0-checksums.txt"
format = "sha256"
