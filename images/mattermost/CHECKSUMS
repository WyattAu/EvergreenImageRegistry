# CHECKSUMS - mattermost
# Generated: 2026-04-20
# Status: PENDING_VERIFICATION
#
# NOTE: This image uses a templated URL. The version is determined at build
# time via the VERSION build argument. Update this file when bumping versions.
#
# IMPORTANT: These checksums must be verified before use.
# Method: Download the binary on an air-gapped machine, compute SHA256,
# then compare against upstream sha256sums.txt if available.
#
# Update protocol:
# 1. Download binary from URL (substitute ${VERSION})
# 2. Compute: sha256sum <file>
# 3. Compare with upstream sha256sums.txt if available
# 4. Cross-validate with second team member
# 5. Update EXPECTED_SHA256 below
# 6. Submit PR with CHECKSUMS update

[metadata]
image = "mattermost"
version = "${VERSION}"
created = "2026-04-20"
last_verified = "PENDING"
verification_method = "PENDING"
verifier = "PENDING"

[download]
url = "https://releases.mattermost.com/${VERSION}/mattermost-team-${VERSION}-linux-amd64.tar.gz"
filename = "mattermost-team-${VERSION}-linux-amd64.tar.gz"

[checksum]
# SHA256 of the downloaded archive/binary
# Leave as "PENDING" until verified
expected_sha256 = "PENDING"

[upstream_checksum]
# If upstream provides a checksum file, note the URL here
url = ""
format = ""
