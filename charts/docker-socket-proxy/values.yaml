# Default values for docker-socket-proxy
image:
  name: docker-socket-proxy
  tag: "latest"
  pullPolicy: IfNotPresent

tier: standard
replicas: 1

security:
  readOnlyRootFs: true

service:
  type: ClusterIP
  port: 8080

env: {}
envFrom: []

persistence:
  enabled: false
