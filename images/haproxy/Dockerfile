FROM scratch
LABEL org.opencontainers.image.title="HAPROXY" \
      org.opencontainers.image.description="Sovereign hardened HAPROXY (stub - upstream binary unavailable at version 3.0.1)" \
      sovereign.image.status="no-upstream-binary" \
      sovereign.image.note="No pre-built binary available for version 3.0.1. Update VERSION when upstream publishes new release."
