# images/keycloak/README.md --> Quick Start Section --> Should be saved as .env.template

# ====================================================================
# Evergreen Image Registry: Keycloak Configuration Template
#
# INSTRUCTIONS:
# 1. Copy this file to a new file named '.env' (`cp .env.template .env`)
# 2. Edit the '.env' file with your secure, custom values.
#
# This file defines all the environment variables used by the docker-compose.yml.
# ====================================================================

# --------------------------------------------------------------------
# REQUIRED: Keycloak Admin Credentials
# --------------------------------------------------------------------
# The initial super-administrator for the 'master' realm.
# IMPORTANT: Change these to strong, unique values.
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=V3ryS3cureP@ssw0rd!

# --------------------------------------------------------------------
# REQUIRED: PostgreSQL Database Connection
# --------------------------------------------------------------------
# These variables MUST be consistent for both the Keycloak and PostgreSQL services.
POSTGRES_DB=keycloak
POSTGRES_USER=keycloak
POSTGRES_PASSWORD=An0therS3cureP@ssw0rd

# --------------------------------------------------------------------
# RECOMMENDED FOR PRODUCTION: Hostname and Proxy Settings
# --------------------------------------------------------------------
# The public-facing domain name for your Keycloak instance.
# Example: KC_HOSTNAME=auth.example.com
KC_HOSTNAME=localhost

# Set to 'edge' when Keycloak is running behind a reverse proxy (like Nginx).
KC_PROXY=edge

# --------------------------------------------------------------------
# DEVELOPMENT ONLY: HTTP Settings
# --------------------------------------------------------------------
# Set to 'true' to allow Keycloak to be accessed over HTTP.
# This should NEVER be true in a production environment.
KC_HTTP_ENABLED=true
